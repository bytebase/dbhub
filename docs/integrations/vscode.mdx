---
title: "VS Code"
---

<Tabs>
  <Tab title="Cline">
    [Cline](https://github.com/cline/cline) is a VS Code extension that supports MCP servers.

    **Installation**

    1. Install the Cline extension from the VS Code marketplace
    2. Configure MCP settings in Cline's settings UI or edit the config file

    **Configuration**

    Edit `~/.cline/mcp_servers.json`:

    ```json
    {
      "mcpServers": {
        "dbhub": {
          "command": "npx",
          "args": [
            "@bytebase/dbhub",
            "--transport",
            "stdio",
            "--dsn",
            "postgres://user:password@localhost:5432/dbname"
          ]
        }
      }
    }
    ```
  </Tab>

  <Tab title="Continue">
    [Continue](https://continue.dev) is another VS Code extension with MCP support.

    **Installation**

    1. Install Continue from the VS Code marketplace
    2. Configure MCP in Continue's settings

    **Configuration**

    Edit `~/.continue/config.json`:

    ```json
    {
      "mcpServers": {
        "dbhub": {
          "command": "npx",
          "args": [
            "@bytebase/dbhub",
            "--transport",
            "stdio",
            "--dsn",
            "postgres://user:password@localhost:5432/dbname"
          ]
        }
      }
    }
    ```
  </Tab>
</Tabs>

## Workspace Configuration

For workspace-specific database connections, create `.vscode/mcp_config.json`:

```json
{
  "mcpServers": {
    "workspace-db": {
      "command": "npx",
      "args": [
        "@bytebase/dbhub",
        "--config",
        "./dbhub.toml"
      ]
    }
  }
}
```

## Troubleshooting

1. Check the extension's output panel for errors
1. Verify DBHub is installed: `npx @bytebase/dbhub --help`
1. Test the DSN connection outside VS Code
1. Restart VS Code after configuration changes